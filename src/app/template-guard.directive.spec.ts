import { TemplateGuardDirective } from './template-guard.directive';

describe('TemplateGuardDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateGuardDirective();
    expect(directive).toBeTruthy();
  });
});
