import { ToggleComponentDirective } from './toggle-component.directive';

describe('ToggleComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleComponentDirective();
    expect(directive).toBeTruthy();
  });
});
